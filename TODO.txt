+ C++20 reflow...
+ -abcdef support arguments
+ -t any argument after this is considered part of the query so that options are not parsed
+ methods.cpp: support multiple arguments like [ "-h", "--help" ] to verify if it exists/get
+ multi-thread: that is, it will run all methods at the same time and stops if finds an answer.
+ multi-thread; that is, it will run all sources at the same time and stops if finds an answer.
+ cache: add a result with the same option and what it found there.
+ Eliminating groups methods by the query itself.
+ + If there is "lyrics" on it, then run only on a subset of websites.
+ + If there is "calculate" on it, then run only the math methods.
@ Joke Classe about input
+ CACHE handling and AI prototype;
+ + Save information locally / server so that queries are send to this file first to see if it can find something
